version: '3.8'

services:
  postgres:
    image: 'postgres:14.3-alpine'
    container_name: lesson2
    restart: always
    ports:
      - "5431:5432"
    environment:
      - POSTGRES_USER=testMan
      - POSTGRES_PASSWORD=123123
      - POSTGRES_DB=y_lab